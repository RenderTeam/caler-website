extends layout

block content
  br
  .container
    .hero-unit.text-center
      h1 Contacto
      
    .custom-narrow
      form.well.span10.white( method = 'POST' , action = 'contacto.php' )
        .row
          .span3
            label Nombre:
            input(type="text", class="span3", placeholder="Nombre", name="nombr" required='required')
            label Correo electrónico:
            input(type="text", class="span3", placeholder="correo@correo.com", name="elcorreo", required='required')
            label Asunto: 
              select.span3(id='asunto', name='asunt' ,required='required')
                option(value='', selected='')  Escoge uno:
                option('cotización')  Cotización
                option('informe') Informes
                option('sugerencias') Sugerencia
            label Curso:
            input(type="text", class="span3", placeholder="Nombre del curso a solicitar información" , name="curso")

          .span7
            label Mensaje
            textarea(name="message", id="message", name="elmsg", class="input-xlarge span7", rows="10")
          button(type="submit", class="btn btn-primary pull-right", name="enviar") Enviar

block custom-scripts
  //for development. We use local scripts
  script(src='javascripts/angular/angular.min.js')
  script(src='javascripts/jquery/jquery.min.js')
  //- for production. We use cdn minified scripts.
      tab back twice to add them to the page and comment 
      or delete the local links
      script(src='//ajax.googleapis.com/ajax/libs/angularjs/1.0.5/angular.min.js')
      script(src='//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js')